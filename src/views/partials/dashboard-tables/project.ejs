<section class="dashboard-section" data-tab="projects">
  <div class="dashboard-table-header">
    <h2>Projects</h2>
    <% if ((user.role === 'admin' && user.canCreateOrders) || (user.role ===
    'client')) { %>
    <a
      href="/dashboard?action=create&entity=project&tab=projects"
      class="btn btn-primary btn-md"
      >Add New Project</a
    >
    <% } %>
  </div>
  <table class="dashboard-table dashboard-table-main">
    <thead>
      <tr>
        <th>Project ID</th>
        <th>Client</th>
        <th>Status</th>
        <th>Total</th>
        <th>Actions</th>
      </tr>
    </thead>
    <tbody>
      <% projects.forEach(p => { %>
      <tr>
        <td><%= p.project_id %></td>
        <td><%= p.client_name %></td>
        <td><%= p.job_status %></td>
        <td><%= p.total_amount %></td>
        <td>
          <% if (user.role === 'admin') { %> <% if (user.canEditOrders) { %>
          <a
            href="/dashboard?action=edit&entity=project&id=<%= p.project_id %>&tab=projects"
            class="btn btn-secondary btn-md"
            >Edit</a
          >
          <% } %> <% if (user.canDeleteOrders) { %>
          <button
            type="button"
            class="btn btn-danger btn-md"
            data-action="delete"
            data-entity="project"
            data-id="<%= p.project_id %>"
          >
            Delete
          </button>
          <% } %>
          <a
            href="/project/status/<%= p.project_id %>"
            class="btn btn-info btn-md"
            >Change Status</a
          >
          <a
            href="/project/payment/<%= p.project_id %>"
            class="btn btn-success btn-md"
            >Update Payment</a
          >
          <% } %> <% if (user.role === 'client') { %>
          <a
            href="/project/view/<%= p.project_id %>"
            class="btn btn-info btn-md"
            >View</a
          >
          <a
            href="/project/payment/<%= p.project_id %>"
            class="btn btn-success btn-md"
            >View Payment</a
          >
          <% } %>
        </td>
      </tr>
      <% }) %>
    </tbody>
  </table>
</section>
