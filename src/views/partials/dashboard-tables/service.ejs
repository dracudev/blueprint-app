<section class="dashboard-section" data-tab="services">
  <div class="dashboard-table-header">
    <h2>Services</h2>
    <% if (user.role === 'admin' && user.canCreateProducts) { %>
    <a
      href="/dashboard?action=create&entity=service&tab=services"
      class="btn btn-primary btn-md"
      >Add New Service</a
    >
    <% } %>
  </div>
  <table class="dashboard-table dashboard-table-main">
    <thead>
      <tr>
        <th>Name</th>
        <th>Actions</th>
      </tr>
    </thead>
    <tbody>
      <% services.forEach(s => { %>
      <tr>
        <td><%= s.service_name %></td>
        <td>
          <% if (user.role === 'admin') { %> <% if (user.canEditProducts) { %>
          <a
            href="/dashboard?action=edit&entity=service&id=<%= s.service_id %>&tab=services"
            class="btn btn-secondary btn-md"
            >Edit</a
          >
          <% } %> <% if (user.canDeleteProducts) { %>
          <button
            type="button"
            class="btn btn-danger btn-md"
            data-action="delete"
            data-entity="service"
            data-id="<%= s.service_id %>"
          >
            Delete
          </button>
          <% } %> <% } %>
        </td>
      </tr>
      <% }) %>
    </tbody>
  </table>
</section>
